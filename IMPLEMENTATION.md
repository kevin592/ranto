# RANTO 官网改版交付说明

当前工作分支：`codex/ranto-brand-redesign`。本次改版在独立 worktree 中完成，原项目目录及其未提交修改保留。本说明记录本地交付状态：**未推送、未部署**。

## 页面与结构

采用 React + TypeScript + Vite，六个独立 HTML 入口由一次构建统一生成；继续兼容 GitHub Pages 的 `/ranto/` 子路径。

| 页面 | 地址 | 内容 |
| --- | --- | --- |
| 首页 | `index.html` | 品牌主张、首发产品与生活场景 |
| 品牌故事 | `story.html` | 围绕日常家居护理重写的品牌理念 |
| 产品系列 | `products.html` | 多功能清洁剂、浴室清洁剂、内衣洗衣液及详情 |
| 品质理念 | `quality.html` | 产品理解、标签及使用信息 |
| 国际视野 | `global.html` | 国际品牌愿景、泰国首发与四语浏览 |
| 联系我们 | `official.html` | 产品与商务咨询入口，移除演示式渠道核验 |

导航、页脚及语言逻辑共用；支持英语、中文、泰语和日语，并记忆访客选择。默认页面元信息使用英文，分享图片采用真实产品原图及当前官网的绝对网址；资源检查仅接受这三张产品图的指定官网地址。

## 预览、构建与发布

使用 Node.js 22：

```sh
npm ci --registry=https://registry.npmjs.org --no-audit --no-fund
npm run dev
```

开发预览默认端口为 3000；以下命令执行完整检查并启动生产预览，默认端口为 4173：

```sh
npm run check
npm run preview
```

可单独运行 `npm run lint`、`npm run build`、`npm run verify:build`。资源检查覆盖六页标识、元信息、本地脚本/样式/图片/字体引用，以及生成的 JS 模块和 CSS 资源。已用独立副本验证：将品牌故事页指向不存在的旧脚本哈希时，检查会失败。

`dist` 已退出 Git 跟踪，本地构建文件仍保留，并由现有忽略规则排除。CI 使用锁定依赖重新构建，检查通过后才上传本次完整产物；PR 仅构建验证，只有 `main` 推送或在 `main` 手动触发才部署。不要手工修改哈希或混合不同构建的页面。

## 素材与信息边界

- `multipurpose.png`、`bathroom.png`、`lingerie.png` 是用户提供的三张原图副本，SHA-256 已逐一核对一致，未改变瓶型、标签或包装。页面将它们作为计划上市包装的产品预览。
- `lifestyle.png` 是本次生成的生活场景配图，用于表达品牌氛围；它不代表真实工厂、客户场地、合作关系或实拍产品证明。
- 页面显示使用同名 WebP 优化副本，原始 PNG 继续保留用于产品详情与分享。两份 Logo 及整个首发素材目录均保留。
- 已核对当前源码与六页入口，移除未引用的 27 个旧图片文件及两个旧图库目录，包括旧六品类概念图、补充装、工厂与酒店等场景图；未修改原项目目录。
- 三款产品均按 500 ml、即将上市展示。使用方法和注意事项以最终销售包装为准，不从视觉参考图推导未经确认的功效或测试结论。
- 品牌故事采用价值与生活方式表达；国际拓展作为愿景，不新增未经资料支撑的日本起源、历史年份、认证、客户或全球渠道背书。
- 对外发布前由业务方确认最终包装与产品说明、上市日期、正式购买渠道，以及现用邮箱 `hello@ranto.co.th` 的接收与承接状态。

本轮已完成六页生产版直达与刷新、桌面排版、手机24个页面与语言组合、语言记忆、菜单、产品详情、键盘焦点循环与联系链接检查。四语各135个文案字段均非空且结构一致；减少动效样式已检查。生产预览使用 /ranto/ 子路径，24个实际构建文件的HTTP响应与本地文件一致。Vite预览对不存在的地址可能回退首页，检查未把这些HTML响应误当作有效脚本。

## 白色瓶身更新
按用户后续要求，内衣洗衣液统一改为白色瓶身效果图。网站使用 lingerie-white-v2.png / lingerie-white-v2.webp，原始米白色图片仍保留。首页、详情及分享图保持同一版本。图像使用内置图像编辑工具完成；具体提示词见 docs/ASSETS.md。

白瓶二次校色已以另外两款喷雾瓶为参照：减轻右侧灰暗阴影，消除偏蓝色调，保留自然立体感。
